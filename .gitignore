# secrets & local state — never commit
.local/
.secrets/
config.json
*.key
.env
.env.*

# runtime artifacts
artifacts/
runs/
reports/
logs/
*.log
preview/
cases/

# python
__pycache__/
*.pyc
.venv*/
.pytest_cache/

# node
node_modules/

# os / editor
Thumbs.db
.DS_Store
nul

# T-206 — 보안 스캔 도구 산출물 (gitleaks 임시 리포트 등)
gitleaks-report.json
gitleaks-report.*
.scan-*

# 임시 주입 검증 파일 (acceptance 검사 후 즉시 삭제되지만 안전망)
*.secret-injection-test
/tmp-injection-*
