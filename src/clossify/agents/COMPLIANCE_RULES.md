# {STORE_NAME} 상품 자동등록 — 준수 규칙 (Single Source of Truth)

> 두 에이전트(네이밍·비전검수)와 파이프라인이 따르는 마스터 규칙. 세션 누적 합의를 고정.
> 이 문서가 규칙의 source-of-truth. 코드/에이전트는 여기를 참조.

> **기준일·범위 안내**(본 문단은 아래 규칙의 해석을 바꾸지 않는 메모):
> 이 규칙은 특정 시점 해석이며 플랫폼 정책·관련 법령 변경 시 갱신이 필요하다.
> 규제 신고값(원산지·인증·고시)의 최종 확인은 **판매자가** 한다.

## 0. 브랜드
- 브랜드명: **{STORE_NAME}** / 콘셉트: **셀렉트샵** / 태그라인: `DESIGN SELECT`
- 톤: 차분한 셀렉트샵. 차콜/화이트.

## 1. 금지어 (HARD — 위반 시 BLOCK)
- **정품 / AUTHENTIC / 100% / 정식 / 공식(가공식품 등 제외) / 최고 / 1위 / BEST(영문) / 최저가 / 초특가 / 역대급 / 국내 유일 / 세계 최초 / 업계 1위 / 무조건 / 최상급 / 완벽 / 프리미엄 / 진품** 등 단정·과장·배타주장.
- 이유: 사실 기반 표현 외에 단정·과장·배타주장은 상품명·상세·이미지 어디에도 금지.
- 비고(코드와 맞춤):
  - 한글 **베스트** 는 조끼(의류 카테고리)와 충돌하여 제외했다. 영문 **BEST** 만 금지.
  - **공식** 뒤에 **품** 이 오는 경우(가공식품·축산가공식품 등 정상 명사)는 금지에서 제외했다.
  - `정식` 은 결정론 정규식에서 제외 — `한정식`·`가정식`·`정식 도시락` 같은 정상 식품 상품명과 구별할 수 없다. 카피 생성 규율(§본문 금지)에서 다룬다.
  - `1위상품`·`100위권` 처럼 **`위` 뒤에 한글이 바로 붙는 형태는 정규식이 잡지 못한다** — `1위생용품` 같은 정상어와 구별할 수 없기 때문이다. 잡는 척하지 않는다.

## 2. 상세페이지 레이아웃 (HARD 락)
- **적용대상 = 상세(desc) 본문 이미지에만.** (옵션 선택 그리드는 §5가 관할 — 다열 허용, 위반 아님)
- 상세 본문 이미지: **단일 컬럼, width 100% 풀폭, 한 줄에 한 장, 세로로만 나열.**
- **금지(상세 본문 한정)**: 좌우배치 / 멀티컬럼 / masonry / grid 다열 / 이미지 축소해 옆에 붙이기.
- **판정 방식 = 코드 결정론**: 렌더된 `detail.html`에서 첫 `photo-stack`부터 `옵션 선택` 섹션 전까지 본문 영역만 검사한다. 이 영역에 `options-grid` / `display:grid` / `grid-template` / `column-count>1` / `flex-wrap` 마크업이 있으면 레이아웃 FAIL, 없으면 PASS.
- **비전 QA 제외**: 비전 모델은 본문 레이아웃을 눈으로 판단하지 않는다. 레이아웃 위반 여부는 HTML 코드검사 결과만 사용한다.
- 이미지 사이사이 **한글 기능설명** 블록.
- **원본 합성이미지 예외**: 원본 이미지 1장 안에 내부 grid/2x2 합성이 들어 있어도, 우리가 그 이미지를 풀폭 1장으로 올린 경우는 레이아웃 위반이 아니다. 우리가 쪼개서 grid 마크업으로 배치했는지만 코드로 판정한다.
- **옵션 그리드는 예외**: `옵션 선택` 섹션은 다열(2~N) 정상. QA는 옵션 그리드를 레이아웃 위반으로 보지 말 것.

## 3. 이미지 처리
- **제품이 보이면 전부 keep** (text/size_chart/notice여도). 순수 텍스트/공지/로고/blank만 제외.
- **이물 외국어 텍스트는 제거**: 사용자가 제공한 이미지에 외국어 라벨이 섞여 있다면 한국어로 정리하거나 덮는다. 단 상품 본연의 식별 정보(브랜드 로고 등)는 훼손하지 않는다.
- **스튜디오샷/흰배경/배경제거/크롭 변환 절대 없음.** 비율 유지, 사이즈 축소만.
- 최종 모든 이미지는 판매용 썸네일 품질 기준 통과.
- **과도 흰여백 트림(HARD)**: 이미지 상/하(좌우)에 **균일한 흰/거의흰 빈 여백이 과도**하면(예 높이의 12%↑) 자동 크롭. 단 제품이 잘리면 안 됨(여백만). 트림 후 살짝(균등) 패딩만.
- **이미지 스케일 불변식(HARD — 근본규칙)**: 상세 캔버스 폭은 1000 고정. **제품 콘텐츠 폭은 [600 ~ 920] 밴드로 클램프**(`fit_to_canvas_width`가 유일 권한). 상한 920<1000이라 **항상 양쪽 흰 margin** — 콘텐츠가 캔버스 끝에 안 닿음(깔끔한 미감):
  - 네이티브 >920 → 920으로 **축소** + 필러박스. · 600~920 → 네이티브 유지 + **흰 필러박스**(가운데). · <600 → **600까지 완만 업스케일**(상한 2.0x, 920 절대 안 넘음) + 필러박스.
  - **너무 작은 원본 스킵**: 원본 최장변 <360px = 썸네일/아이콘 → **밴드에 안 넣고 제외**(뭉개느니 버림). 단 옵션 4×N 그리드는 별개(옵션 관할).
  - **절대 금지: 가는 조각을 풀폭으로 몇 배 업스케일**(트림이 만든 360px 화병을 1000으로 2.77배 부풀려 거대·픽셀깨짐 — 과거 버그).
  - **순서**: 트림은 정규화 *전에만*, fit은 항상 *마지막* 단계(fit 뒤 재-트림 금지). 정리 후 트림했으면 **다시 fit**해 복귀.
  - **렌더 경계 강제(fail-closed)**: HTML 진입점(`build_detail_html`)에서 전 타일 `enforce_canvas_tiles` 통과 — 비순응 재-fit, 못 맞추면 **드롭**. 회귀 가드 `detail_tile_scale_violations`가 폭!=1000 잡아 자동 재-fit + QA 기록. 무음 통과 금지.
  - 옵션 썸네일 CSS=`object-fit:contain`(cover 금지 — crop/확대 방지). render 스크립트 기본폭=1000.

## 4. 카피 — 제목=기능, 본문=감성 허용 (하이브리드)
- **역할 분리**: 발견은 키워드로(제목), 구매결정은 감성으로(본문). 인테리어/소품은 감성구매 카테고리.
- **SEO 제목**: 기능·키워드만(§7). 감성표현 금지(제목 한정).
- **본문 카피**: **무드·감성 넉넉히 허용**(욕망 자극) + 기능·활용 녹임(스펙→혜택, [[COPY_GUIDE]] 훅/3단). "시간의 흔적이 느껴지는" "은은한 멋" 류 무드 표현 OK — 상품맥락에 닿으면 환영. 너무 빡빡하게 보지 말 것.
- **길이(HARD): 짧고 펀치있게.** 상세는 이미지라 모바일에서 긴 문단 금지. **밴드당 1~2문장**(리드 최대 3문장). **instructional/SEO 필러 꼬리 금지** — "~상세 이미지와 옵션에서 확인해 주세요", "크기·소재·사용 위치를 확인" 같은 안내문구·키워드 나열을 본문에 붙이지 말 것(그건 옵션/고시가 담당). 한 문장에 한 메시지.
- **본문 금지(좁게)**: ① **내용 0의 빈 마케팅 문구**("일상에 특별함을", "당신만을 위한" 류 어느 상품에나 붙는 공허한 말) ② **가짜통계**(재구매율·판매량 등 수치 지어내기) ③ 과장·정품주장(§1). → 이 셋만 NG, 나머지 감성은 통과.

## 5. 옵션
- **번호 매칭**: optionName `1.`,`2.` ↔ 상세 그리드 `①②③` (단일소스 option_number_records, 드롭다운=라벨 일치).
- **옵션명 = 사용자가 입력한 한국어 명칭 그대로 유지**. 코드형 옵션(MDYZ01 등)이 섞여 있으면 그대로 보존. 커스텀/템플릿화 금지.
- **품절 반영**: 재고 0인 옵션은 stock 0.
- **옵션 레이아웃 적응형**: 옵션이미지 비율 따라감(세로 길면 세로로, 크롭X). 열수 적응(2~N). 길어져도 됨.
- 옵션이미지 없으면: 번호+한글명+설명+계열톤 카드(폴백).
- **옵션 사이즈(범용 규칙 — 전 상품 동일)**: 사이즈는 *추출만, 지어내기·수동입력 절대 0*. 소스 우선순위 = 옵션이미지 OCR > 스펙시트 OCR > props (생 텍스트를 LLM이 포맷무관 읽음, 판매자별 파서 금지). **있으면 자동 표기, 없으면 그냥 "사이즈 정보 없음".** 추정·placeholder·수동요청 안 함.

## 6. 헤더/푸터 (HARD 락 — 고정 상수)
- {STORE_NAME} 헤더 + 배송/교환/안내 푸터 = **상수 블록 고정**. 매번 재생성 금지.
- **헤더에 카테고리 subtitle 금지**(예 "일반스탠드" 잘못 박힘). 고정 태그라인만.
- SEO 제목 한 줄만 변수, 나머지 전부 상수.

## 7. SEO 상품명
- **검색되는 유형·용도·재질·스타일 앞배치.** ~50자, 반복단어/특수문자/이모지 금지, §1 금지어 금지.
- **검색량이 낮거나 의미 없는 단어 제거**: 고유명사·작가명·브랜드코드·오카테고리 용어는 **제목에서 빼고 본문 스토리로 강등.**
- **검색량 기준**(네이버 검색광고 키워드 API 연동 시): 검색량 0인 단어 컷, 높은 순 앞배치.
- **카테고리 relevance(HARD)**: 키워드는 *이 제품의 유형·용도·재질·스타일* + *인접 인테리어 맥락*(예 화병에 '그릇세트'·'장식소품'=OK)만. **명백히 다른 카테고리의 스펙용어는 검색량 높아도 컷** — 예: 화병에 '조도/루멘/밝기'(조명), '용량/소비전력'(가전), '사이즈275'(신발). 오카테고리 용어는 검색품질 저해 + 고객 혼동 → qa_copy FAIL.
- 자연스러운 한국 쇼핑 한국어.
- **네이버 상품명 공식**: `[카테고리] [핵심키워드] [제품명] [추가스펙/장점]`. **앞단어일수록 가중치 큼** → 최고검색어를 맨 앞. (네이버 랭king = 적합도[앞키워드]·인기도[판매·리뷰]·신뢰도[고객보호정보])
- **속성/고시 필드 100% 채움**(소재·색상·크기…) — 적합도 점수 직결, 빈칸 금지.

## 8. 원산지·인증
- 원산지=사용자가 입력한 실제 원산지(국가명). KC 필요 여부는 `category_meta.requires_kc(category_id)` 로 판정 — 필요 카테고리면 인증정보 표기, 불필요면 미표기. 카테고리=네이버 leaf(`data/category_meta.json` 매칭).

## 9. 용어집 (오카테고리·직역 방지)
인테리어 소품 용어는 검색에 쓰이는 자연스러운 한국어로 통일: 장작가마, 다도 트레이/차판, 잔받침, 주전자받침, 인테리어소품, 수제 단품, 무드등/아로마등, 침실등, 소금램프/암염등.

## 10. 폰트 사이즈 (HARD — 상세는 이미지라 모바일 기준 크게)
상세페이지는 이미지를 폰에서 축소해 보므로 웹 UI 크기(16~18px) 금지. **가로 1000px 렌더 기준 최소 크기:**
- 상품명/메인: **56~70px** · 섹션 헤딩(옵션선택·상품정보·DETAIL 소제목): **40~46px**
- **본문/설명: 35~40px** (40 권장) · 캡션·상품정보 값: **26~30px** · 라벨/eyebrow/각주: **22~24px**
- 옵션명·번호배지: 28~34px. 가격(있으면): weight 800, tabular-nums, 자간 -0.02em.
- 폰트=**Pretendard**(Variable). 자간 -0.02~-0.04em(헤딩 더 타이트), 본문 행간 1.45~1.5(촘촘하되 한글 가독). 좌우 여백 ~60px, 섹션 상하 100~160px.
- (근거: 상세 제작 가이드 메인70/서브50/본문35-40/각주20pt @860px)

## 11. 이미지 분할 (HARD — 통짜 금지)
- 한 이미지 세로 **3000px 초과 시 분할 강력권장, 5000px 이하**. 최종 상세가 길면(수만 px) **세로 세그먼트로 분할**해 detailContent에 **여러 장 적층 업로드**.
- 각 이미지: 가로 **860~1000px**, **최대 20MB**, JPEG(투명배경만 PNG). 분할 경계는 섹션 사이(이미지/텍스트 중간 안 자르게).
- 고화질(2x→LANCZOS)은 유지하되, 최종 출력은 ~3000px 단위 다분할.

## 12. 법적 필수 (HARD — 누락 시 과태료 최대 500만원)
- **원산지: 사용자가 제공한 구체적 국가명 필수.** "해외"·"기타" 단독표기 금지. originAreaInfo content 값은 config/payload 일치 기준으로 검증.
- **상품정보제공고시**: 카테고리별 필수필드 채움(ETC라도 소재·크기·제조국·취급주의 등 성실 기재). 빈칸/더미 금지.
- **KC**: `category_meta.requires_kc` 가 True인 카테고리(전기·생활·아동용품 등)는 KC마크+인증번호+유형(안전인증/확인) 표기. False면 미표기.
- **교환·반품 5요소(푸터 배너에 반드시 포함)**: ①청약철회 가능기간 ②반품배송비 부담주체 ③교환·반품 불가사유 ④환불 처리 소요기간 ⑤판매자 연락처. (고정 푸터 detail_footer.png가 이 5개 커버하는지 검수). 판매자 연락처는 {AS_TEL} placeholder로 주입.

## 13. 전환 (판매 베스트프랙티스)
- **스펙 → 혜택**: 사양만 적지 말고 "그래서 좋은 점"을 붙인다(§4 기능카피에 혜택 한 줄). 예: "7.5cm 소형" → "좁은 책상에도 부담 없는 크기".
- **첫 상품 화면(고정 헤더 다음 첫 섹션)**: 핵심 혜택 한 줄 + 베스트 상품컷 먼저. 3초 안에 "뭘 파는지" 읽히게.
- 모바일 76%+ 구매 → §10·§11 준수(폰트 크게·860+px·분할).
- (모델 한계 메모: 리뷰/판매량 소셜프루프·CTA버튼은 신규 상품엔 제한적 — 가능할 때 후기/뱃지 추가.)
